
# enable uv autocompletion
if command -v uv >& /dev/null; then
    eval "$(uv generate-shell-completion $MYSHELL)"
fi

# enable uvx autocompletion
if command -v uvx >& /dev/null; then
    eval "$(uvx --generate-shell-completion $MYSHELL)"
fi
